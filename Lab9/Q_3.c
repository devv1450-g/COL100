#include<stdio.h>
 
void main(){
   int total_money, no_flav;
   scanf("%d%d", &total_money, &no_flav);
   int A[no_flav];
   for(int i=0; i<n; i++){
       scanf("%d", &A[i]);
   }
   int copy[100];
    for(int i=0; i<100; i++){
        copy[i] = -1;
   }
   for(int i=0; i<no_flav; i++){
       copy[A[i]] = i;
       if(total_money-A[i]>0)
   }
   
}